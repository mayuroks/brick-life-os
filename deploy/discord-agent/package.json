{
  "name": "discord-agent",
  "version": "0.1.0",
  "description": "Life OS agent exposed as a Discord surface — bundles the 001 agent (opencode + skills + Jira MCP) headless with a thin Discord bridge",
  "type": "module",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "boot": "node scripts/bootstrap.js",
    "serve": "opencode serve"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.15.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
