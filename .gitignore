# Secrets — never commit
opencode.json
.env
.env.local
*.pem

# Node / deps
node_modules/
/package.json
/package-lock.json
/bun.lock

# Editor / OS
.DS_Store

# Local AWS CLI v2 (installed in-project, not committed)
.aws-cli-v2/
aws.sh
